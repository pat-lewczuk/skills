{
  "id": "long-but-dense",
  "kind": "audit",
  "split": "holdout",
  "target": "AGENTS.md",
  "invoke": "explicit",
  "prompt": "This AGENTS.md is 93 lines and every new joiner complains it is too long. Audit it.",
  "description": "\"Your file is long because your project is complicated\" — the finding the skill's taxonomy calls out as valid. Six domains, almost every line a threshold, a cost, a wrong-default warning or a recovery path, with three planted platitudes. The correct output is a small cut plus a routing-structure recommendation; a large cut here is a failure even though the user asked for one. Holdout.",
  "expect": {
    "rewrite": true,
    "reduction_pct": [0, 25],
    "must_mention": ["routing|split into|per-domain|load(ed)? on demand|nested AGENTS|separate reference"],
    "must_keep_nouns": [
      "poke_interval=300", "WH_DEV_XS", "platform.seed.set_all", "--consent-cohort",
      "s3://atlas-checkpoints/", "make tf-plan", "logical_date", "unique_key",
      "features/_state/", "2 seconds", "#atlas-platform"
    ]
  }
}
