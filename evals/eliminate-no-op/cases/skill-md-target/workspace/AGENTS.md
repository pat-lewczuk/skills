# AGENTS.md

This repo publishes agent skills. Each skill is `skills/<name>/` with a `SKILL.md`.

- `name` in the frontmatter must match the directory name.
- Explanation goes in `references/`; `SKILL.md` says when to read it.
- Scripts are stdlib-only — consuming projects have no install step.
